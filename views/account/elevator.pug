extends ../layout


block content
  .pb-2.mt-2.mb-4.border-bottom
  include ../partials/profilehead.pug
  
  span ELEVATOR - 
  a(href='/account/elevsettings') Settings - 
  a(href='/account/elevatorentrycreate') Add an entry

    hr

    br
  - var userr = { id: user._id }

  - if (elevdata.length)
  
  each items in elevdata 
    if items.username == userr.id 

            strong #{moment(items.elevdate).format('MMM Do')} - #{items.eleventrytitle} #{items.username}
            p #{items.elevcat} #{userr.id}
            a(href='/account/elevator/'+items._id) Edit
            hr

  #elevator
